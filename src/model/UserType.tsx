export type UserType = {
  id: string;
  username: string;
  email: string;
  avatar_url: string;
  role: string;
  status: string;
  refresh_token: string;
  access_token: string;
  student_id: string;
};
